Some objects are simple and can be instantiated in a single constructor call.
However, there are complex objects that needs a lot of ceremony to be created,

> The builder Design Pattern is used to instantiate complex objects in a more productive way,
piece by piece, meeting to the configuration needed.

"When piecewise object construction is complicated, provide an API for doing it succinctly"